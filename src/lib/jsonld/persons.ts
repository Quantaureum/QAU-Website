/**
 * Known Person profiles for JSON-LD attribution.
 *
 * Currently empty: the upstream site inherited third-party profiles that
 * do not apply to Quantaureum, so no individual is credited website-wide.
 * Add real, verifiable entries here if an attribution is ever needed.
 */
export const KNOWN_PERSONS = {} as const
